# os-demo

Pequeña demo web estática (HTML + CSS + JS) con un contador interactivo.

## Cómo verla

Abre `index.html` en el navegador, o sírvela en local:

```bash
python -m http.server 8000
# luego abre http://localhost:8000
```

## Licencia

MIT
